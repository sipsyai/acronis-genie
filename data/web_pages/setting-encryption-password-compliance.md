# Setting the encryption password

Additional Cyber Protection tools > Compliance mode > Setting the encryption password
Setting the encryption password

You must set the encryption password locally, on the protected device. You cannot set the encryption password in the protection plan. Without a password, creating backups will fail.

There is no way to recover encrypted backups if you lose or forget the password.

You can set the encryption password in the following ways:

During the installation of a protection agent (for Windows, macOS, and Linux).

By using the command line (for Windows and Linux).

This is the only way to set an encryption password on a virtual appliance.

For more information on how to set an encryption password with the Acropsh tool, see Encryption.

In the Cyber Protect Monitor (for Windows and macOS).

To set the encryption password in the Cyber Protect Monitor

On the protected device, log on as an administrator.

Click the Cyber Protect Monitor icon in the notification area (in Windows) or the menu bar (in macOS).

Click the gear icon.

Click Encryption.

Set the encryption password.

Click OK.

See also
Installing protection agents by using the graphical user interface
Encryption
Back to Top



Last build date: Tuesday, March 10, 2026

User Guide for Cyber Protect Cloud Console26.02.